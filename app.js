
function deduct(grade,decrement){
    
    var point = grade-decrement;
    
}

function addgrade(grade,increment){
    
    var score = grade+increment;
   
}

module.exports = {
    
    Deducted :deduct,
    Added :addgrade
};